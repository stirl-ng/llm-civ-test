name: gemini-demo
backend:
  kind: gemini
  model: gemini-2.5-flash-lite
  # api_key can be set here or via GEMINI_API_KEY env var
  api_key: AIzaSyDv9HUxEIkaniSJbchwEp8SEVf52Suzxv4
tools:
  - kind: mcp
    # base_url will use orchestrator.url if not specified
  - kind: rag
    corpus:
      - "Civilization V policies: Tradition, Liberty, Honor."
strategy:
  name: vanilla
  temperature: 0.2
orchestrator:
  pipe: "\\\\.\\pipe\\civv_llm"

